[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn \"app:create_app()\" --workers 2 --timeout 120 --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
